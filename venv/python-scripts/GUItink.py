from tkinter import *

root = Tk()

topFrame = Frame(root)
topFrame.pack()
theLabel = Label(root, text="Siem tool python")
theLabel.pack()
bottomFrame = Frame(root)
bottomFrame.pack(side=BOTTOM)
root.mainloop()
